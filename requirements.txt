# Core analysis
pandas>=1.5.0
numpy>=1.23.0
statsmodels>=0.14.0
scipy>=1.9.0
openpyxl>=3.0.0
matplotlib>=3.5.0

# Desktop app (optional - only required for `streamlit run app.py`)
streamlit>=1.30.0

# Notebook kernel (optional - only required to run the .ipynb)
ipykernel>=6.0.0
